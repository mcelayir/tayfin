<!--
Canonical README template for Tayfin modules.
Placeholders ({{...}}) should be replaced by implementers with concrete values.
-->
# {{Module Name}}

## Description
Short, 1–3 sentence summary of the module's responsibility within the Tayfin ecosystem.

Example: "tayfin-ingestor is responsible for ingesting market data from providers, normalizing it, and persisting raw and cleaned records for downstream processing."

## Service Overview
- Responsibility: {{Data Egress/Ingress | Calculation Logic | Filtering | BFF integration}}
- Primary interfaces: {{API endpoints, CLI jobs, message queues}}
- Owner: @{{owner-handle}}

## Placeholder Guidance
Use this short guide when replacing `{{placeholders}}` in the template. Keep values concise and machine-friendly where possible.

- `{{Module Name}}` — Human-readable module title. Example: `tayfin-ingestor`.
- `{{module}}` — short slug used in URLs and API paths. Example: `ingestor` (lowercase, no spaces).
- `{{service_name}}` — runtime service name (used in docker-compose or systemd). Example: `tayfin-ingestor-service`.
- `{{module_entry_point}}` — Python module or CLI entrypoint used to start the service. Example: `tayfin_ingestor.main` or `tayfin_ingestor.__main__`.
- `{{repo_path}}` — repo-relative path to the code for quick navigation. Example: `tayfin-ingestor/tayfin-ingestor-api`.
- `{{job_run_id}}` — provenance token format. If generated by the service, show example pattern (e.g. `job-20260322-abc123`) and note that all writes must include this value.
- `{{owner-handle}}` — GitHub/Slack handle of the owner or team responsible for the README content (e.g. `@dev`).

Notes:
- Mark any example payloads as `illustrative` if they are not verified against live code.
- Never include real secrets or credentials; use placeholders like `${API_TOKEN}` or `REDACTED`.
- Keep placeholders consistent across README files within the same module.

## Getting Started

### Local Commands
| Task | Command | Description |
| :--- | :--- | :--- |
| Start dev | `make run-local` | Starts service with hot-reload (replace with actual command) |
| Run job | `bin/job --type=sync` | Run manual data sync job |
| Run tests | `pytest -q` | Run unit tests |

### Environment Variables
| Key | Required | Description |
| :--- | :---: | :--- |
| `DB_URL` | Yes | SQL connection string for the module database |
| `JOB_RUN_ID` | Yes | Job provenance identifier attached to writes |
| `PROVIDER_API_KEY` | Conditionally | API key for external provider (if used) |

### Execution Examples
- Docker (compose):
```bash
docker-compose -f infra/docker-compose.yml up --build {{service_name}}
```
- Python (venv):
```bash
source .venv/bin/activate
python -m {{module_entry_point}} --config config/local.yml
```

## Local Jobs / Cron
- `jobs/sync_prices.py` — description: pulls prices and writes to raw table.  
- Cron example: `0 * * * * /opt/app/bin/job --type=sync`

## API Documentation

### `POST /v1/{{module}}/ingest`
**Description:** Ingest payload into module.

**Request (application/json):**
```json
{
  "provider": "example_provider",
  "timestamp": "2026-03-22T12:00:00Z",
  "records": [
    {"symbol": "AAPL", "price": 183.45, "volume": 1200}
  ]
}
```

**Response (200 OK):**
```json
{
  "status": "accepted",
  "job_run_id": "{{job_run_id}}",
  "ingested": 1
}
```

**Curl Example**
```bash
curl -X POST https://localhost:8000/v1/{{module}}/ingest \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer ${API_TOKEN}" \
  -d '{"provider":"example_provider","timestamp":"2026-03-22T12:00:00Z","records":[{"symbol":"AAPL","price":183.45,"volume":1200}]}'
```

### `GET /v1/{{module}}/status`
**Response (200 OK):**
```json
{ "status": "ok", "uptime": 12345 }
```

## Request / Response Types
- Use JSON schema or simple type tables here. Example:
| Field | Type | Required | Notes |
| :--- | :--- | :---: | :--- |
| `symbol` | string | Yes | Market symbol |
| `price` | number | Yes | Price in USD |

## Observability
- Logs: include `job_run_id` and request IDs in key flows.  
- Metrics: `requests_total`, `ingest_success_total`, `ingest_failure_total` with tags `module`, `endpoint`.

## Security
- Auth: describe required tokens / internal service auth and scopes.  
- Do not include secrets; use placeholders in examples.

## QA Checklist (to be validated by `@qa`)
- [ ] Curl examples run against local dev or test environment and return expected responses.
- [ ] Job example executes locally and produces expected output.
- [ ] Environment variables table is complete and accurate.
- [ ] No secrets in the README.

## Troubleshooting
- Common errors and remediation steps. Example: DB connection failures, missing API keys.

## Links
- Code: `{{repo_path}}`
- Architecture: `docs/architecture/tayfin_project_documentation.md`

## CHANGELOG
- Note when the README was updated and by whom.

---

Replace all `{{placeholders}}` with concrete values for the module. When adding example payloads, prefer real shapes from the code rather than invented fields — if unavailable, mark examples as "illustrative" and request owner confirmation.
