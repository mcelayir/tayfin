# tayfin-indicator-api configuration
# Environment variables override these values (ADR-04).

server:
  host: "0.0.0.0"
  port: 8010

upstream:
  ingestor_api_base_url: "http://localhost:8000"
  timeout_s: 30.0
